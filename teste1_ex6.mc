int gx, gy;
char gc;

int f(int a, b) { return 0; }
int g(char a, b) { return 1; }

int main() {
    int x;
    char c;
    int x;

    x = 1;
    y = 2;

    c = 'a';
    x = c;

    c += 1;
    x += 2;

    x = c + 1;

    break;
    continue;

    while (x <= 3) {
        x = x + 1;
        if (x >= 10)
            break;
        else
            continue;
    }

    f(1);
    f(1, 2, 3);

    g(1, 2);
    g('a', 'b');

    h();

    return 0;
}